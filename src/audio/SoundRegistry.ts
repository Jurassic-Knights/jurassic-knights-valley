/**
 * Sound Registry
 * Maps sound IDs to audio configuration
 *
 * Owner: SFX Engineer
 */

const SoundRegistry = {
    // Sounds are loaded from assets/registry/audio.json
    // This file can contain runtime overrides or computed mappings
};

export { SoundRegistry };
